import React from "react";

// I can make a custom hook here for this function but for right now I will use the set state method as that would be easiest

export default function Player({props}){

    return(
        <div className="player">
            
        </div>
    )
}